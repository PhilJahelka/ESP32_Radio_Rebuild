Clean up screen prints and clears
Wifi mode can change stations with single button press
Volume buttons
Use SPIFFS for radio station list
  Will need to really rethink the state machine portion; vector of states created during setup function?

For some reason KXLU doesn't work when using SPIFFS to build station list
  Nuking all file stuff from the I2S library doesn't fix it. Hmmmm.